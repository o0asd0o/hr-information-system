export enum LeaveType {
    VACATION_LEAVE = "vacation leave",
    SERVICE_INCENTIVE_LEAVE = "service incentive leave",
    SICK_LEAVE = "sick leave",
}

export enum LeaveStatus {
    PENDING = "pending",
    REJECTED = "rejected",
    APPROVED = "approved",
}